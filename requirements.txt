# Core dependencies
numpy>=1.23.5,<1.24
opencv-python>=4.8
torch>=2.0
Pillow>=9.5
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
decord>=0.6
mediapipe>=0.10
onnxruntime>=1.16

# 六维度评测新增依赖
insightface>=0.7
py-feat>=0.6
timm>=0.9
openai>=1.0
anthropic>=0.20

# Dev tools
pytest>=7.4
ruff>=0.4

# 以下依赖不在 PyPI，需从 third_party/ 或 GitHub 手动安装：
# pip install git+https://github.com/facebookresearch/segment-anything.git
# pip install git+https://github.com/facebookresearch/co-tracker.git
# pip install git+https://github.com/IDEA-Research/GroundingDINO.git
# sam2, q_align 等参见 third_party/ 目录
